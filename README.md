# Akal Karya — Vite React Scaffold

This workspace contains a Vite + React + Tailwind scaffold with your React app moved to `src/App.jsx`.

Quick start (Windows PowerShell):

```powershell
npm install
npm run dev
```

Open http://localhost:5173 to view the app.
